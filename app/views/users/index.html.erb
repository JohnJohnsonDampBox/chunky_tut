<h1>Users</h1>

<ul>
<% @users.each do |user| %>
  <li><%= link_to user.name, user %> - <%= link_to 'Edit', edit_user_path(user) %> - <%= link_to "Delete", user, method: :delete, data: { confirm: "Remove #{user.name}?" } %></li>
<% end %>
</ul>


<p><%= link_to "New User", new_user_path %></p>

